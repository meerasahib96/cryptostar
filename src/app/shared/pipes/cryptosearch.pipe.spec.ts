import { CryptosearchPipe } from './cryptosearch.pipe';

describe('CryptosearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CryptosearchPipe();
    expect(pipe).toBeTruthy();
  });
});
